#python solution for leetocde problem 946
class Solution(object):
    def validateStackSequences(self, pushed, popped):
        result=[]
        num=0
        for i in pushed:
            result.append(i)
            while result  and result[-1]==popped[num]:
                result.pop()
                num+=1
        return len(result)==0
        """
        :type pushed: List[int]
        :type popped: List[int]
        :rtype: bool
        """
        
